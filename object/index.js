const source = {
  a: 1,
  get b() {
    return 2;
  }
}

const res = Object.assign({}, source)

Object.myAssign = function (target, sources) {
  sources.forEach((source) => {
    const de
  })
}

console.log(res)